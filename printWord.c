#include <stdio.h>
#define MAXLINE 1000

int main() {
    int numChar, c, i;
    char ch[];

    numChar = 0;
    i = 0;
    while ((c = getchar()) != EOF) {
        if (c != ' ' || c != '\t' || c != '\n')
            ++numChar;
        else {
            
        }
    }
}
